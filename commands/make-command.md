---
description: "引数から新しいカスタムスラッシュコマンド(.md)を生成するメタコマンド"
argument-hint: "<name> [--scope user|project] [--dir sub/dir] [--desc \"...\"] [--args \"...\"] [--model MODEL] [--tools \"Bash(git ...), ...\"] [--prompt \"本文\"] | <name> :: <本文>"
---

あなたは「/make-command」コマンドです。目的は `$ARGUMENTS` を解析し、指定に基づく新しいカスタムスラッシュコマンドの .md ファイル内容と作成手順を出力することです。**破壊的操作は実行せず、ユーザーが貼り付けて作成できる形で出力**してください。

## 受け付ける書式
- フラグ形式:
  - `name --scope user|project --dir sub/dir --desc "説明" --args "[引数ヒント]" --model MODEL_ID --tools "Bash(git add:*), Bash(git status:*)" --prompt "本文"`
- 省略形:
  - `name :: 本文`
- 既定値:
  - scope = `user`
  - dir = なし
  - desc = 本文の先頭行
  - args = なし
  - model / tools = 省略時は frontmatter に出さない

## 生成ルール
1) **コマンド名** = `name`。`[a-z0-9][a-z0-9-_]*` に正規化（空白は`-`に）。
2) **保存先パス**:
   - scope=`user` → `~/.claude/commands/{dir?}/{name}.md`
   - scope=`project` → `.claude/commands/{dir?}/{name}.md`
3) **frontmatter** は次のキーのみ、値があるものだけを出力順で記述:
   - `description`, `argument-hint`, `model`, `allowed-tools`
4) **本文** は `--prompt` または `::` 以降のテキストをそのまま使用。`$ARGUMENTS` や `$1` 等が含まれていればそのまま残す。
5) **出力順**（必ずこの順で、わかりやすい見出しを付ける）:
   - 📁 **出力先パス**
   - 📝 **新規ファイルの内容**（```md で囲む）
   - 💻 **作成コマンド**（```bash で `mkdir -p` と `cat > ... <<'MD'` による保存ワンライナー）
   - ✅ **使い方の例**（`/help`に表示される説明、呼び出し例）
6) 既存ファイルと衝突しそうなら、上書き注意の一言も載せる。

## 解析と既定値の適用
- フラグ形式は `--key value` / `--key "value with spaces"` を優先的に解釈。
- `name :: 本文` 形式のときは `name` を取り、残りを本文にする。
- `argument-hint` はそのまま frontmatter に入れる（補完に出ます）。
- `tools` はそのまま `allowed-tools` に入れる（省略時は出力しない）。

## 出力スタイル
- コードはコピーしやすい最小限。
- 説明は箇条書きで端的に。

## ここから `$ARGUMENTS` を処理
1) `$ARGUMENTS` から `name`, `scope`, `dir`, `desc`, `args`, `model`, `tools`, `prompt` を抽出。
2) ルールに従って .md 内容と作成コマンドを構築。
3) 指定が不完全でも安全な既定値で補完し、完成した出力を返す。
